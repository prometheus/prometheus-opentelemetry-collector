apiVersion: v1alpha1
sts-region: us-east-1

discovery:
  exportedTagsOnMetrics:
    AWS/EC2:
      - Name
  jobs:
    - type: AWS/EC2
      regions:
        - us-east-1
      metrics:
        - name: CPUUtilization
          statistics:
            - Average
          period: 300
          length: 300
